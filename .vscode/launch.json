{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run DaysOfJFE",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/DaysOfJFE",
            "cwd": "${workspaceFolder}"
        }
    ]
}